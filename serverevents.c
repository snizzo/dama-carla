#include "serverevents.h"

int registerNewPlayer(char * nickname, char * password)
{
	printf("trying to register %s with password %s \n", nickname, password);
	//instantly generating output
	fflush(stdout);
	return 0;
}
